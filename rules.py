def bond_score(a: DigitalAtom, b: DigitalAtom, env=None) -> float:
    # Stub: combine oxidation-state match, electronegativity gap, valence completion
    # Return a score; threshold decides if bond forms
    return 0.0
